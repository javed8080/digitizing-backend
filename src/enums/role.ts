export enum Role {
    superAdmin = "super-admin",
    admin = "admin",
    subAdmin = "sub-admin",
    customer = "customer",
    digitzer = "digitzer",
    vector = "vector",
    sales = "sales",
    manager = "manager"
}

export enum Destination {
    digitizer = "digitizer",
    teamLead = "team-lead",
    salesMan = "sale-man"
}
